name: RepoPalette
author: onovich
description: Generate a validated GitHub language SVG and auditable JSON in your own repository.

inputs:
  style:
    description: "Visual layout: bars, orbit, constellation, ribbon, bead-halo, matrix, halo, treemap, voronoi, or prism."
    required: false
    default: bars
  theme:
    description: "Color theme: light, paper, midnight, aurora, terminal, or neon."
    required: false
    default: light
  username:
    description: GitHub account whose public, owned repositories are analyzed.
    required: false
    default: ${{ github.repository_owner }}
  github-token:
    description: GitHub API token. The automatic workflow token is used by default; no PAT is needed for public repositories.
    required: false
    default: ${{ github.token }}
  output-directory:
    description: Repository-relative directory for the generated SVG and audit JSON.
    required: false
    default: assets
  top:
    description: Number of languages displayed, from 1 to 12.
    required: false
    default: "6"
  include-archived:
    description: Include archived repositories in the statistics.
    required: false
    default: "false"
  exclude-repositories:
    description: Comma-separated repository names to exclude.
    required: false
    default: ""
  exclude-languages:
    description: Comma-separated language names to exclude.
    required: false
    default: ""
  title:
    description: Title rendered in the SVG.
    required: false
    default: Most Used Languages
  width:
    description: SVG width from 320 to 800 pixels.
    required: false
    default: "400"
  show-branding:
    description: Show a small RepoPalette watermark in each SVG.
    required: false
    default: "true"
  coding-mode:
    description: "Programming-method display: off for one chart, or split for separate Manual Coding and Vibe Coding charts. Classification is always user-declared."
    required: false
    default: off
  manual-languages:
    description: "Comma-separated languages declared as manually coded when coding-mode is split. Every remaining language is placed in the Vibe Coding chart."
    required: false
    default: ""

outputs:
  svg-path:
    description: Absolute path to the single SVG when coding-mode is off; empty in split mode.
  manual-svg-path:
    description: Absolute path to the Manual Coding SVG in split mode; otherwise empty.
  vibe-svg-path:
    description: Absolute path to the Vibe Coding SVG in split mode; otherwise empty.
  data-path:
    description: Absolute path to the generated audit JSON.
  repository-count:
    description: Number of public, owned repositories returned by GitHub.
  included-repository-count:
    description: Number of repositories included after filters.
  language-count:
    description: Number of languages in the audit data.

runs:
  using: node24
  main: src/action.mjs

branding:
  icon: bar-chart-2
  color: purple
